package main

// it tell tthat it is the main starting point of the function
// func main() {
// 	fmt.Print("Kya haal hai ")
// }

// to run the go run file name

// go build  it

// cannot find main module, but found .git/config in /home/saurabh/Desktop/Go-Maximilan
// to create a module there, run:
// // cd .. && go mod init

// it  gives the error because it menas a go project it could

// go mod init turn this project into module

// go mod init exaample.com/first-app it create the files

// it generates the first app

// run ./first-app  -- it runs the app without anyone to install the go

// thats the reason that we require packages

// main tells the go compiler that this is the main package of the application for here the code runs

// only main will run as the default code
